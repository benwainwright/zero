export { User, type IUser } from '@user';
export {
  type IActor,
  type IDomainEvents,
  type IEvent,
  DomainModel,
} from '@core';
export {
  type IPermission,
  type ICapability,
  permissionSchema,
} from '@permission';
export { Role, type IRole, roleSchema } from '@role';
