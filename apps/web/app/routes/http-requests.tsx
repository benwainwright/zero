import { HttpRequestsPanel, Page } from '@components';

export const HttpRequests = () => {
  return (
    <Page routeName="httpRequests">
      <HttpRequestsPanel />
    </Page>
  );
};

export default HttpRequests;
